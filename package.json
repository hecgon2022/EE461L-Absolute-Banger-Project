{
    "scripts": {
        "frontend": "cd materialUIFrontEnd && npm start",
        "start": "cd materialUIFrontEnd && react-scripts start",
        "build": "cd materialUIFrontEnd && react-scripts build"
    }
}